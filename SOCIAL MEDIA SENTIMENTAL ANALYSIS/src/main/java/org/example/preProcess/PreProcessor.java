package org.example.preProcess;

import java.io.*;

public class PreProcessor {
    public File reFormatData(File file) throws IOException {
        BufferedReader bf = new BufferedReader(new FileReader(file));


        return file;
    }
}
